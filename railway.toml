# Railway configuration for soul-kiln main application
# https://docs.railway.com/reference/config-as-code

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
numReplicas = 1
